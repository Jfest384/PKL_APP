@page "/map"
@inject IJSRuntime JS

<h3>Peta Lokasi Saat Ini</h3>

<div id="map" style="height: 500px;"></div>

<button @onclick="ShowMap">Tampilkan Lokasi</button>

@code {
    private async Task ShowMap()
    {
        await JS.InvokeVoidAsync("showMapWithCurrentLocation");
    }
}
